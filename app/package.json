{
  "name": "ricos-epos-app",
  "private": true,
  "version": "0.1.0",
  "description": "Sunmi T2 Android wrapper (Capacitor) for the Rico's / Food Station staff EPOS. One APK, provisioned per device to a shop. Self-contained — does not affect the web build.",
  "type": "module",
  "scripts": {
    "test": "node scripts/native-test.cjs",
    "sync-web": "node scripts/sync-web.mjs",
    "prepare:android": "npm run sync-web && cap add android",
    "sync": "npm run sync-web && cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/preferences": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
