{
  "name": "ricos-peri-peri",
  "version": "0.1.0",
  "private": true,
  "description": "Rico's Peri Peri online ordering — Cloudflare Pages + Functions.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-05-01 --kv ORDERS_KV --kv MARKETING_KV --kv SLOTS_KV --kv STAFF_LOGIN_KV",
    "deploy": "wrangler pages deploy public --project-name=ricos",
    "tail": "wrangler pages deployment tail --project-name=ricos"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
